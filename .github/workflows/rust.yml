on:
  pull_request:
    branches:
      - main
    paths-ignore:
      - 'docs/**'
      - 'README.md'
      - '.gitignore'
      - 'Makefile'

name: Check, Test, and Format

jobs:
  fmt:
    if: true
    name: Format
    runs-on: ubuntu-latest
    steps:
      - run: sudo apt-get update && sudo apt-get install -yq pkg-config libssl-dev
      - uses: actions/checkout@v4
      - uses: actions-rs/toolchain@v1
        with:
          profile: minimal
          toolchain: nightly-2024-04-20
          override: true
      - run: rustup component add rustfmt
      - uses: actions-rs/cargo@v1
        with:
          command: fmt
          args: --all -- --check

  test:
    if: true
    name: Tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout sources
        uses: actions/checkout@v4
      - name: Install toolchain
        uses: actions-rs/toolchain@v1
        with:
          profile: minimal
          toolchain: nightly-2024-04-20
          override: true
      - uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/bin/
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            target/
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
      - name: Cargo build examples
        run: RUSTFLAGS="-Awarnings" cargo build --examples --release
      - name: Cargo run examples
        run: |
          RUST_LOG="info" RUSTFLAGS="-Awarnings" FRI_QUERIES="1" cargo run --release --example toy_base_machine
          RUST_LOG="info" RUSTFLAGS="-Awarnings" FRI_QUERIES="1" cargo run --release --example toy_simple_machine
          RUST_LOG="info" RUSTFLAGS="-Awarnings" FRI_QUERIES="1" cargo run --release --example toy_lookup_machine
          RUST_LOG="info" RUSTFLAGS="-Awarnings" FRI_QUERIES="1" cargo run --release --example test_simple_machine keccak
          RUST_LOG="info" RUSTFLAGS="-Awarnings" FRI_QUERIES="1" cargo run --release --example test_simple_recursion
          RUST_LOG="info" RUSTFLAGS="-Awarnings" FRI_QUERIES="1" cargo run --release --example test_riscv_emulator
          RUST_LOG="info" RUSTFLAGS="-Awarnings" FRI_QUERIES="1" cargo run --release --example test_riscv_machine keccak
          RUST_LOG="info" RUSTFLAGS="-Awarnings" FRI_QUERIES="1" cargo run --release --example test_riscv_compress
          RUST_LOG="info" RUSTFLAGS="-Awarnings" FRI_QUERIES="1" cargo run --release --example test_riscv_combine